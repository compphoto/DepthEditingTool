import React from "react";
import { render } from "@testing-library/react";
import DepthViewer from ".";

test("renders DepthViewer", () => {
  render(<DepthViewer />);
});

import { render } from "@testing-library/react";
import MainPane from ".";

test("renders MainPane", () => {
  render(<MainPane />);
});
